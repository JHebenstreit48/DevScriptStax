import { Subpage } from "@/Navigation/NavigationTypes";

const RustNavigation: Subpage = {
  name: "Rust",
  subpages: [
          { name: "Rust Notes", path: "/rust" },
          // { name: "Rust Code Notes", path: "/rust/rustcode" },
  ],
};

export default RustNavigation;
