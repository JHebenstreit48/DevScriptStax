import { Subpage } from "@/Navigation/FrontEndNavigation/NavigationTypes";

const AANavigation: Subpage = {
  name: "A + A",
  subpages: [
    { name: "Auth Notes", path: "/auth" },
    { name: "Auth Code Notes", path: "/auth/authcode" },
  ],
};

export default AANavigation;
